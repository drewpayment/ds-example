import { ServiceStartEndFrequency } from './service-start-end-frequency';

describe('ServiceStartEndFrequency', () => {
  it('should create an instance', () => {
    expect(new ServiceStartEndFrequency()).toBeTruthy();
  });
});
