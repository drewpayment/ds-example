import { ServiceCarryOverFrequency } from './service-carry-over-frequency';

describe('ServiceCarryOverFrequency', () => {
  it('should create an instance', () => {
    expect(new ServiceCarryOverFrequency()).toBeTruthy();
  });
});
