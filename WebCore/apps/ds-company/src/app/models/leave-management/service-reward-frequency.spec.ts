import { ServiceRewardFrequency } from './service-reward-frequency';

describe('ServiceRewardFrequency', () => {
  it('should create an instance', () => {
    expect(new ServiceRewardFrequency()).toBeTruthy();
  });
});
