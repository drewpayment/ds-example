import { AutoApplyAccrualPolicy } from "./auto-apply-accrual-policy";

describe('AutoApplyAccrualPolicy', () => {
  it('should create an instance', () => {
    expect(new AutoApplyAccrualPolicy()).toBeTruthy();
  });
});
