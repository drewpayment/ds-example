import { ServiceCarryOverWhenFrequency } from './service-carry-over-when-frequency';

describe('ServiceCarryOverWhenFrequency', () => {
  it('should create an instance', () => {
    expect(new ServiceCarryOverWhenFrequency()).toBeTruthy();
  });
});
