import { AccrualBalanceOption } from './accrual-balance-option';

describe('AccrualBalanceOption', () => {
  it('should create an instance', () => {
    expect(new AccrualBalanceOption()).toBeTruthy();
  });
});
