import { AccrualCarryOverOption } from "./client-accrual-carry-over-option";

describe('AccrualBalanceOption', () => {
  it('should create an instance', () => {
    expect(new AccrualCarryOverOption()).toBeTruthy();
  });
});
