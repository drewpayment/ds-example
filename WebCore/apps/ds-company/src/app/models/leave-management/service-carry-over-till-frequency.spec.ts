import { ServiceCarryOverTillFrequency } from './service-carry-over-till-frequency';

describe('ServiceCarryOverTillFrequency', () => {
  it('should create an instance', () => {
    expect(new ServiceCarryOverTillFrequency()).toBeTruthy();
  });
});
