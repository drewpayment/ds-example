import { GetCellTitlePipe } from './get-cell-title.pipe';

describe('GetCellTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCellTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
