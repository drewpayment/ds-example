export interface PunchType {
    punchNames: PunchName[];
}

export interface PunchName {
    clockName: string;
    firstName: string;
    lastName: string;
    employeeId: number;
    name: string;
    count: number;
}