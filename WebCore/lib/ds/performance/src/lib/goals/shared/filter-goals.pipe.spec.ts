import { FilterGoalsPipe } from './filter-goals.pipe';

describe('FilterGoalsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGoalsPipe();
    expect(pipe).toBeTruthy();
  });
});
