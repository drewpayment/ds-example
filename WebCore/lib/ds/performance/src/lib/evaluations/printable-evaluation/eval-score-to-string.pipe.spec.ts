import { EvalScoreToStringPipe } from './eval-score-to-string.pipe';

describe('EvalScoreToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EvalScoreToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
