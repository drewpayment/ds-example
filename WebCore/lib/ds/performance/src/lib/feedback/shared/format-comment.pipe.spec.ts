import { FormatCommentPipe } from './formatted-comment.pipe';

describe('FormatCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
