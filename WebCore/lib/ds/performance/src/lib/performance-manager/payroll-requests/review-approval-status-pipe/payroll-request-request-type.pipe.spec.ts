import { PayrollRequestRequestTypePipe } from './payroll-request-request-type.pipe';

describe('PayrollRequestRequestTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PayrollRequestRequestTypePipe();
    expect(pipe).toBeTruthy();
  });
});
