import { DsExpansionContentDirective } from './ds-expansion-content.directive';

describe('DsExpansionContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DsExpansionContentDirective();
    expect(directive).toBeTruthy();
  });
});
