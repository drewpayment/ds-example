import { TrackChangesDirective } from './track-changes.directive';

describe('TrackChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackChangesDirective();
    expect(directive).toBeTruthy();
  });
});
