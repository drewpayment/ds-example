import { CustomControlValidatorDirective } from './custom-control-validator.directive';

describe('CustomControlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomControlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
