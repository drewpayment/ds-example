import { TrackFormChangesDirective } from './track-form-changes.directive';

describe('TrackFormChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackFormChangesDirective();
    expect(directive).toBeTruthy();
  });
});
