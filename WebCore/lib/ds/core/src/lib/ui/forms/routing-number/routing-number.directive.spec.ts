import { RoutingNumberDirective } from './routing-number.directive';

describe('RoutingNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutingNumberDirective();
    expect(directive).toBeTruthy();
  });
});
