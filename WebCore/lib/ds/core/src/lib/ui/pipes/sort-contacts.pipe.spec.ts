import { SortContactsPipe } from './sort-contacts.pipe';

describe('SortContactsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortContactsPipe();
    expect(pipe).toBeTruthy();
  });
});
