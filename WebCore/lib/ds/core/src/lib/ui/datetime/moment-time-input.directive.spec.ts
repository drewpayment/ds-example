import { MomentTimeInputDirective } from './moment-time-input.directive';

describe('MomentTimeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MomentTimeInputDirective();
    expect(directive).toBeTruthy();
  });
});
