import { ToAutocompleteItemPipe } from './to-autocomplete-item.pipe';

describe('ToAutocompleteItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ToAutocompleteItemPipe();
    expect(pipe).toBeTruthy();
  });
});
