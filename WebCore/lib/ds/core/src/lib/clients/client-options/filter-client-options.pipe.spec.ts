import { FilterClientOptionsPipe } from './filter-client-options.pipe';

describe('FilterClientOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
