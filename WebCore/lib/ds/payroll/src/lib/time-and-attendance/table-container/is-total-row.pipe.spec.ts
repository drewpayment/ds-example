import { IsTotalRowPipe } from './is-total-row.pipe';

describe('IsTotalRowPipe', () => {
  it('create an instance', () => {
    const pipe = new IsTotalRowPipe();
    expect(pipe).toBeTruthy();
  });
});
